apiVersion: 1

providers:
  - name: supply-chain
    folder: Supply Chain OS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
